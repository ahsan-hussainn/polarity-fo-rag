# Copy to .env (gitignored) and fill in. Never commit real credentials.

# Supabase Postgres connection string.
# Dashboard -> Connect -> "Session pooler"  (IPv4, port 5432).
#   - NOT "Direct connection"    -> IPv6-only, unreachable from most local machines.
#   - NOT "Transaction pooler"   -> port 6543, breaks psycopg's prepared statements.
# Replace [YOUR-PASSWORD] with the database password you set when creating the project.
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres

# Stage 3 extraction LLM (ADR-0008). Provider is swappable behind the extract() seam.
OPENAI_API_KEY=sk-...
# EXTRACT_PROVIDER=openai        # openai | mock   (default: openai)

# Email verification API (ADR-0005 fallback / ADR-0010). Swappable behind the verifier seam.
# Free key at millionverifier.com -> API. Used by: validate-emails --verifier millionverifier
MILLIONVERIFIER_API_KEY=...
# EMAIL_VERIFIER=millionverifier # millionverifier | mock
