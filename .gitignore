# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.python-version

# Node
node_modules/
.next/
dist/
build/

# Secrets and env
.env
.env.*
!.env.example
*.key
credentials.json
service-account*.json

# Data working files (raw scratch; validated deliverable is committed deliberately)
data/raw/
data/scratch/
*.tmp

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# The operating-ledger export is a GENERATED deliverable (python -m pipeline.cli ops-export).
# It is regenerated fresh at submission and committed then, rather than churning ~1.5MB of JSONL
# into history on every run. The ledger itself lives in Postgres; this is a dump of it.
data/ops_export/
