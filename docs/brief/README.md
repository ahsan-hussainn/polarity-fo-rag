# The Stage 2 brief, in-repo

`Differentiator-Stage-2-v9-2026.docx` is the assessment brief as received (2026-07-27). The `.txt`
beside it is a plain-text extraction of the same file, committed so the brief is greppable and so
any claim in this repo can be checked against its exact wording without opening Word.

It lives here because the mandate is read from it constantly and every session needs the same
source of truth. Prior sessions referenced it only by an absolute path on one machine
(`D:\Polarity IQ Final Round\Stage 2\...`), which is not a shared artifact.

**Precedence:** the brief outranks `docs/OPERATING_PLAN.md`, every ADR, and every note in this
repo. Where they disagree, the brief is right and the other document is drift to be fixed. Three
such drifts were found and resolved on day 2 — see `docs/findings/stage2-brief-reconciliation.md`.

**Do not edit either file.** They are the received document, not working notes.
